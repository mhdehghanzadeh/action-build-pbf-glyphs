name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        uses: mhdehghanzadeh/action-build-pbf-glyphs
        with:
          source: fonts/
          target: build/fonts/
